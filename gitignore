# .gitignore

# Secrets — never commit these
.env

# Model artifacts — too large for GitHub, regenerated by train.py
ml/model.pkl
ml/preprocessor.pkl
ml/feature_columns.pkl
ml/shap_summary.png

# Data — Kaggle datasets don't belong in the repo
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Local DB (SQLite fallback)
*.db

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
